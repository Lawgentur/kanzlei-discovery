# Kanzlei-Recherche Prozess-Update

- **Quelle:** Nur noch Google Sheet (target_firms.csv).
- **Format:** Alle Ausgaben als CSV, UTF-8 codiert, mit korrekten Umlauten.
- **Bereitstellung:** Nur als Download (MEDIA:), keine großen Textblöcke im Chat.
- **Backup-Handling:** Neue Backups werden in `backups/` entpackt und in die bestehende `jobs_master.csv` gemergt, ohne Duplikate zu erzeugen.
