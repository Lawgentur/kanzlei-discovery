# Archive

This folder keeps historical project artifacts for reference.

- `legacy-scripts/`: one-off import, merge, conversion, and simulation scripts from the earlier workflow.
- `scraper-broken/`: the old OpenClaw-era scraper. It contains useful ideas, but the Python file is not executable because it was corrupted with pasted response text.
- `openclaw/`: workspace metadata from the retired OpenClaw setup.

Production code now lives in `src/kanzlei_discovery/`.

