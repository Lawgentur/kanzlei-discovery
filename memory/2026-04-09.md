## 2026-04-09
- **Project "Kanzlei Discovery" Update:** The user runs a scraper at `/home/ubuntu/kanzlei_discovery/` via cron (Sundays), producing data in `data/` including a 3MB `jobs_master.csv`.
- **Infrastructure Note:** User is accessing OpenClaw via `myclaw.ai` (no direct SSH/filesystem access). Delivered a large CSV export by uploading it to gofile.io.
- **Current Status:** User wants to enrich the firm database with a custom list of domains. The chat text was too long and truncated. I created an extraction script (`/home/ubuntu/kanzlei_discovery/check_new_domains.js`) and am waiting for the user to upload the full list as `input_domains.txt` into the workspace to finalize the cross-referencing and start scraping the new URLs.
